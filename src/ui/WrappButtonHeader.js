import React, { Component } from 'react';
import styled, { css } from 'styled-components';

const WrappButtonHeader = styled.div`
  display: inline;
  align-items: center;
`;

export default WrappButtonHeader;
