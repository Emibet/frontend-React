import React, { Component } from 'react';
import styled, { css } from 'styled-components';

const WrappSignUp = styled.div`
  display: inline-flex;
  flex-direction: column;
`;

export default WrappSignUp;
